import re

with open("frontend/src/pages/TransactionDetail.tsx", "r") as f:
    text = f.read()

# I will replace the main layout structure in the component to better reflect the prompt's requested sections.
# I'll build a script that reads the API data and constructs the appropriate JSX.
